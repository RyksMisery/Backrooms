# A/B-бот света — единая инструкция

Продуктовый `level_e` с `2026-07-19` всегда стартует в `LF3-11F`. Reference и
исторические `10J/11P` применяются ботом программно на время кадра и после
теста не считаются игровыми режимами. Для звукового регресса старый генератор
доступен запуском `--level-e-reference-audio`; обычного игрового переключателя
старого света или звука нет.

Контроль продуктового default: `2026-07-19 02-35-33`. Сцена стартует в
`LF3-11F + FINAL WAV`, бот временно применяет Reference/10J и возвращает
финальный профиль. Vulkan stress прошёл, среднее время кадра
`REFERENCE/10J/11F = 14.179/14.049/14.053 ms`.

Пространственный area-pool проверяется отдельно:

```text
Godot --headless --path . --script res://tools/validate_level_e_spatial_light_pool.gd
```

Validator проходит одинаковые центры областей вперёд и назад, сравнивает
энергию, range, visibility и shadow-state, затем повторяет неподвижную
позицию. Любое направленное расхождение или изменение при остановке считается
ошибкой. Контроль `2026-07-27`: 16 позиций, `0` расхождений, 30 неподвижных
повторов без изменения.

При активном эксперименте model-only fill запуск
`--lf3-level-e-box-shadow-capture` обязан дополнительно сохранить фиксированную
пару коробок `model_fill_off/on/contact` и метрики при неизменном `LF3-11F`.
Основные shadow-sequences выполняются после восстановления исходного состояния
fill. Таблички у провала проверяются тем же ручным переключателем `4`, поскольку
их оценка требует обхода двух разных позиций.

Переключатель model-only fill намеренно не удаляется после текущего допуска:
он служит единым A/B-харнессом для будущих 3D-объектов. Новые receivers должны
получать отдельную фиксированную пару `OFF/ON` или явный ручной ракурс; нельзя
оценивать объект по двум прохождениям с разными лампами, камерой или LF3-пулом.

Первый model-fill отчёт: `.lf3_level_e_boxes/2026-07-19 03-18-02`;
коробки `off/on ROI luma = 0.19152/0.19231`, `RGB MAE=0.000902`.
Сопутствующий Vulkan stress `.lf3_level_e_shadow_smoothness/2026-07-19
03-18-59`: `LF3-11F mean=14.244 ms`, `max=28.822 ms`, device loss нет.

Для receiver-first эксперимента box-smoothness бот обязан сравнить
`LF3-11F ↔ LF3-11R` при одной фиксированной камере. В каждом sample поле
`receiver_direction` содержит ближайшую к коробкам достигающую лампу,
ближайшую активную теневую лампу, флаг совпадения и dot направлений. Итоговый
отчёт отдельно считает кадры, где ближайшая предметная лампа не получила
shadow-slot. Это диагностика выбора источника; визуальный контактный лист всё
равно остаётся обязательным.

Контроль `2026-07-22 23-50-22/23-53-50`: оба профиля дали
`nearest_source_shadowed=81/81` на коробках, то есть текущая траектория не
ловит заявленную обратную тень. `11R` не превысил 11 shadow-cubemap и не вызвал
device loss, но во втором stress занял `28.92 ms` против `26.65 ms` у `11F` и
дал mean leak-risk `1.904` против `1.858`. Результат отрицательный для смены
default; профиль сохранён только для нового прогона с точным проблемным
ракурсом.

Для `LF3-11A` тот же box-smoothness runner обязан добавить секцию `rotation`:
неподвижные игрок и камера вращаются по одной дуге в направлениях CW и CCW.
Каждый sample хранит угол, angular-weight активных источников, число теней,
step RGB MAE и leak-risk. Контактные листы `lf3_11f_rotation_*` и
`lf3_11a_rotation_*` проверяют боковое появление теней; совпадающие углы двух
направлений обязаны давать одинаковые shadow-signature и изображение.

Контроль `2026-07-23 00-14-46/00-18-36/00-22-25`: CW/CCW дали `0`
несовпадений signature, Vulkan не потерял устройство, максимум — `11` теней.
Stress leak-risk `11F/11A`: mean `1.858/1.803`, max `2.979/2.259`.
Положительный результат по засвету пока не перевешивает повторяемый рост frame
time; `11A` не назначен default. Ручной запуск кандидата:
`Godot --path . level_e.tscn -- --lf3-angular-shadow-test`.

Для `LF3-11G` обязательны два независимых запуска на фиксированном maze seed
`173205`:

1. `--lf3-level-e-box-shadow-smoothness-capture` сравнивает `11F/11G` при
   подходе, отходе и CW/CCW-повороте в зале;
2. `--lf3-level-e-capture` сравнивает `11F/11G` в четырёх maze-ракурсах и на
   одинаковом 900-кадровом поперечном маршруте.

Maze-отчёт обязан хранить отдельный stress для каждого профиля, leak-risk по
кадрам и shadow-signature в контрольных ракурсах. Нельзя прогонять профили
последовательно одним неразмеченным stress: результат должен явно относиться
к `11F` либо `11G`. Ручной запуск кандидата:
`Godot --path . level_e.tscn -- --lf3-guardian-shadow-test`.

Первый maze-прогон `2026-07-23 00-54-03` является отрицательным checkpoint для
coarse guardian-cache: `11F/11G mean leak-risk = 0.00835/0.00965`, max
`0.1139/0.2765`; `maze_north_cross` изменился с `0` до `0.08137`. Этот вариант
не возвращать. Повторный допуск выполняется только после перехода на
lamp-to-receiver-cell topology cache.

Повторные допуска topology-cache:

- зал `2026-07-23 01-11-23`: mean frame `11F/11G = 18.398/16.819 ms`, mean
  leak-risk `1.858/1.809`, max `2.979/2.563`, CW/CCW signature mismatches `0`;
- maze `2026-07-23 01-02-01`: mean frame `14.991/15.010 ms`, динамический
  mean/max leak-risk совпадает точно, `south_wide` leak `0.072943 → 0`, прочие
  контрольные ракурсы без регресса.

Ручное сравнение в `level_e`: `0` переключает `LF3-11F ↔ LF3-11G`, HUD
показывает активное имя. Переключатель не заменяет автоматические прогоны и не
меняет продуктовый default нового запуска.

Для изоляции дрожания мягких omni-теней используется отдельный запуск:

```text
Godot --path . level_e.tscn -- --lf3-level-e-shadow-stability-lab
```

Он не входит в обычный игровой режим и не меняет default. Для каждого
варианта runner обязан:

1. прогреть профиль и зафиксировать одну камеру большого зала;
2. снять неподвижную серию без движения игрока и камеры;
3. повторить одинаковый CW/CCW-поворот;
4. сохранить RGB/luma относительно штатного `LF3-11F`;
5. выполнить размеченный stress без GPU-readback и записать mean/p95/max;
6. проверить лимит shadow-caster и occupancy leak-risk.

Замороженный shadow-пул обязан захватываться только после штатного расчёта
`11F`; direct light, энергия и `pool_want` не замораживаются. TAA, blur,
positional filter quality и atlas задаются в отчёте явно и восстанавливаются
после теста. Отсутствие device loss в одном запуске не считается достаточным:
лучший кандидат повторяется минимум три раза и затем проходит обычные
box/maze-прогоны.

Результат LF3-S `2026-07-27`:

- полная матрица: `.lf3_level_e_shadow_stability/2026-07-27 00-58-36`;
- blur/quality/atlas: `2026-07-27 01-04-38`;
- три повтора финальной четвёрки:
  `01-12-30`, `01-15-56`, `01-19-15`;
- maze в прямом/обратном порядке:
  `.lf3_level_e_shadow_stability_maze/2026-07-27 01-23-57/01-26-10`.

Направление для ручной оценки — `LF3-11G`, `blur=2.125`, TAA `OFF`, filter
quality `2`, atlas `4096`. Максимальный CW/CCW RGB MAE стабильно снизился
примерно на `65%`; динамический maze leak-risk совпал с `11F`, статические
четыре ракурса кандидата дали нулевой leak-risk, максимум — 11 теней,
device loss — `0`. Заморозка пула, TAA, high-quality filter и atlas `8192`
отклонены по совокупности плавности, изображения, цены и риска памяти.

Два SIGABRT при попытке запуска maze около `00:47` произошли до старта движка
в macOS AppKit `_RegisterApplication`, пока был открыт редактор Godot. Это не
Vulkan device loss и не относится к световому профилю. Графические боты при
открытом редакторе запускать отдельным экземпляром macOS (`open -n`).

Этот файл — каноническая инструкция для автоматического сравнения света в
лаборатории `infinite_corridor_e.tscn`. История экспериментов и причины
решений остаются в `docs/lighting_field.md`; устройство самой лаборатории — в
`docs/infinite_corridor_e.md`. Если старые разделы используют имена LF2 или
`.lf2_captures`, приоритет у этого файла.

## Что сравнивается

- `LEGACY` — неизменённый референсный свет Godot.
- `LF3` — текущий экспериментальный режим.
- Статический бот проверяет одинаковые камеры, состояния лампы, геометрию,
  среднюю яркость, RGB-расхождение и установившееся время кадра.
- Динамический бот проверяет движение, direct/shadow-пулы, неподвижную
  стабильность, рециклинг чанков, скачки энергии и frame time.
- Бот не принимает художественное решение. Ощущение глубины, естественность
  затухания и характер света подтверждаются человеком после машинного допуска.

## Ручное управление лабораторией

| Клавиша | Действие |
|---|---|
| `8` | атомарно переключает `REFERENCE LEGACY ↔ EXPERIMENT LF3` |
| `9` | включает/выключает LF3 indirect, не меняя LEGACY |
| `7` | игровая камера и пять фиксированных A/B-ракурсов |
| `6` | `LIVE → BRIGHT 1.00 → DIM 0.08` |
| `5` | запускает полный статический A/B-прогон |
| `4` | общий model-only fill стула и дальнего стоп-знака `OFF/ON` |
| `1/2` | уменьшает/увеличивает энергию model-only fill |

Обычный запуск `infinite_corridor_e` стартует в финальном `LF3`; `LEGACY`
доступен только сравнением на `8`. После автоматического или ручного прогона
бот обязан вернуть игровую камеру, `LIVE`, исходное состояние indirect,
HUD/миникарту и тот режим света, который был активен до запуска.

## Статический прогон

Предпочтительный запуск:

```text
Godot --path . infinite_corridor_e.tscn -- --lf3-capture
```

Совместимый исторический alias `--lf2-capture` пока сохранён. Клавиша `5`
запускает тот же сценарий из игры.

Бот снимает:

1. коридор;
2. коридор → комната;
3. комната → коридор;
4. угол / перегородка;
5. стул;
6. физически закрытая дверь;
7. отдельную пару LF3 indirect `OFF/ON`.
8. отдельную пару model-only fill стула `OFF/ON`;
9. отдельную пару model-only fill дальнего стоп-знака `OFF/ON`.

Для основных видов формируется сетка: столбцы `LEGACY | LF3`, строки
`BRIGHT | DIM`. Выходной каталог:

```text
.lf3_captures/<timestamp>/
```

Он содержит исходные PNG, контактные листы и `report.json`. Каталог
диагностический и игнорируется git.

Главные поля отчёта:

- `captures[].mean_frame_ms` — среднее время установившегося кадра;
- `pair_metrics[].luma_ratio` — отношение средней яркости LF3/LEGACY;
- `pair_metrics[].rgb_mae` — средняя абсолютная RGB-разница;
- `indirect_toggle_metrics` — изолированный вклад indirect.
- `chair_fill_metrics` и `stop_sign_fill_metrics` — изолированный вклад
  model-only fill при общей энергии и неизменном LF3-профиле.

Контроль дальнего знака: `.lf3_captures/2026-07-19T03-32-28`;
`off/on luma = 0.296084/0.296172`, `RGB MAE=0.000091`. Контактный лист
`09_stop_sign_fill__contact.png` имеет порядок `OFF | ON`.

## Динамический прогон

Запуск:

```text
Godot --path . infinite_corridor_e.tscn -- --lf3-motion-capture
```

Контроль без indirect:

```text
Godot --path . infinite_corridor_e.tscn -- --lf3-motion-capture --no-indirect
```

Runner переводит сцену в пост-свопный бесконечный коридор, прогревает рендер,
держит игрока неподвижно 30 контрольных кадров, затем проходит 100 м шагом
0.5 м. Во время движения GPU-readback изображений запрещён: иначе скриншот
сам создаёт измеряемый лаг.

Выходной каталог:

```text
.lf3_motion/<timestamp>/
```

В нём находятся `report.json` и `frame_times.png`. На графике отмечаются
обычные кадры, смены direct-пула, shadow-пула и рециклинг.

Главные поля отчёта:

- `median_ms`, `p95_ms`, `max_ms`;
- `stationary_max_visible_energy_delta`;
- `max_visible_energy_delta`;
- `direct_changes`, `shadow_changes`, `recycle_frames`;
- покадровые `z`, `cycle_count`, ID пулов и `indirect_profile`.

## Диагностика пробоя через перегородку

### Подвижная перегородка стандартной области

Лаборатория `light_leak_distance_lab.tscn`, дистанционная A/B-матрица и
дальний угловой sweep описаны в `docs/light_leak_distance_lab.md`.
Rendered-команды:

```text
Godot --path . --script res://tools/run_light_leak_distance_ab.gd
Godot --path . --script res://tools/run_light_leak_corner_sweep.gd
```

Угловой runner обязательно сравнивает прямой/обратный проход по одинаковым
углам, shadow-signature и RGB MAE. Профили рекомендуется запускать отдельными
процессами через `--variant=<name>`: совпадающие хэши PNG разных camera
transform означают зависший framebuffer и делают остаток длинного прогона
недействительным.

Для связного открытого проёма используется `--open-control`; `--fine` проходит
ту же дугу с шагом `1°`. Отчёт отдельно считает luma-step на смене
shadow-signature и без неё, чтобы перспективное появление яркого проёма в ROI
не считалось мерцанием shadow-pool.

Результат `2026-07-28`: opacity-guard уменьшает засвет, но усиливает шаг при
view-dependent смене caster. Для закрытой двери/заглушки принят лабораторный
кандидат occupancy light-zone cull: dark-кадр совпадает с ambient, смен
shadow-signature нет; при открытом проёме штатный свет не меняется. В
продуктовый `level_e` кандидат ещё не перенесён.

Следующий A/B-кандидат — `segment_guardian`: cache штатного caster-набора и
occupancy opacity-guard на время нахождения игрока в одной клетке. Для допуска
обязательны открытый проём, fine sweep с шагом `1°`, неизменная
caster-signature при неподвижном игроке, максимум `11` теней и отсутствие
renderer-сбоя при завершении процесса.

Лабораторный допуск `2026-07-28`: sealed fine-sweep
`2026-07-28T16-55-45` дал `0` смен caster и max-step `0.00004743`; open fine-
sweep `2026-07-28T16-54-39` также дал `0` смен. Открытый distance-контроль
`2026-07-28T16-55-25` отличается от штатного света примерно на `0.07%`.
Следующая обязательная проверка перед продуктовым переносом — движение через
границы occupancy-клеток.

После ручной проверки `segment_guardian` по-прежнему оставляет диагональный
засвет. Bot-only контроль `risk_all_shadow` включает тени у всех источников с
ненулевым LF3 occupancy-risk. Его задача — установить, вызван ли остаток
источниками вне бюджета `10+1`, и измерить необходимое число shadow caster;
это не допустимый продуктовый профиль.

Прогон `2026-07-28T17-17-13`: диагональный вид потребовал `19…21` теней,
снизил luma почти до ambient, но занял примерно в `1.8` раза больше времени и
дал `21` смену signature. Контроль отклонён; причина засвета подтверждена.

Следующая матрица `spot_shadow` сравнивает направленные вниз SpotLight с
кубическими Omni: угол `55…85°`, энергия по сохранению lit/open luma. Допуск:
не более `5%` отклонения полезного света, диагональный leak на уровне
all-risk, отсутствие пятен конусов и renderer-сбоев.

Отдельно `risk_all_shadow_dp` сравнивает те же Omni с dual-paraboloid
shadow mode. Runner обязан проверить диагональный fine-sweep, открытый проход,
lit luma, шов при вращении и время прогона относительно `LF3-11F`.

Гибрид `lf3_spot_fallback` оставляет выбранные `10+1` LF3 Omni и заменяет
SpotLight только источники без тени. Он проходит те же lit/open/leak проверки.

Finalist `70° ×8`: sealed fine `2026-07-28T17-34-48` дал `0` смен и
max-step `0.00001821`; open fine `2026-07-28T17-35-38` также дал `0` смен.
Lit ROI отличается не более чем на `3%`, открытый проход — примерно на
`0.4%`. Кандидат допущен только к ручному тесту клавишей `F`; `C` остаётся
приседанием игрока.

После продолжительного ручного finalist-теста получен
`VK_TIMEOUT / Lost VkDevice`. Обязательный motion-runner
`run_light_leak_spot_motion.gd` запускает `dark/lit × lf3/hybrid` отдельными
процессами и связывает кадровые скачки с occupancy-клеткой и сменой
`Omni/Spot` signature.

Motion-результат `2026-07-28`: hybrid дал `17` смен ролей в тёмной и `21` в
светлой части, исключительно на границах occupancy-клеток. Прямой/обратный
маршрут выбрал разные role-наборы в `14/25` и `10/25` одинаковых позиций.
Кандидат отклонён: тип лампы нельзя переключать дискретно по клетке.

Fixed `11 Omni + 10 Spot` дал `0` role-change в dark, но lit-процесс потерял
renderer на обратном маршруте. Следующая матрица снижена до постоянных
`7 Omni + 14 Spot`; обязательны полный lit-motion и длительный stress.

Static `7/14` снял role-switch, но не восстановил верх стен. Матрица продолжена
однородным `bidirectional_spot`: down/up энергии подбираются по lit ROI,
после чего повторяются open/leak/motion/stress.

`bidirectional_spot` отклонён: верхняя полусфера не восстанавливает потолок и
верх стен без пересвета center/mean. Принят лабораторный
`lf3_occlusion_suppress`: он сохраняет Omni и `10+1`, а энергия
незащищённого blocked-источника комплементарна текущей LF3 shadow-opacity.
Sealed corner достиг ambient с `0` rotation-switch, светлая ROI отличается
менее чем на `0.12%`, шесть последовательных dark/lit motion-stress процессов
завершились без renderer error и forward/reverse mismatch.

Этот допуск отозван после ручного маршрута `dark_wall_opening`: движение
вдоль стены при постоянном взгляде на проём показывает переключения, которые
общий motion-маршрут маскировал изменением перспективы. Runner обязан
проверять отдельный `--route=dark_wall_opening` и сохранять energy-signature
всех Omni; нулевого forward/reverse mismatch недостаточно, если signature
меняется внутри маршрута.

`zone_static_11` прошёл новый критерий: на точном маршруте и шести повторных
stress-процессах `signature_changes = 0`, `role_changes = 0`,
`max_source_energy_step = 0`, все mirror mismatch равны нулю. Предыдущий
position-dependent профиль на том же маршруте дал `10` role-change и скачок
энергии полного диапазона `1.0`.

Универсальный модуль дополнительно проходит data-тест транспонирования:
горизонтальная и вертикальная перегородки с одинаковой occupancy-топологией
обязаны давать транспонированные зоны, портал, caster-набор и одинаковые
energy/opacity-профили. Запуск:

```text
Godot --headless --path . \
  --script res://tools/validate_light_zone_profile_module.gd
```

Rendered universal-профиль дополнительно проходит все четыре позиции
перегородки, точный `dark_wall_opening` и `portal_crossing`. На portal crossing
максимальный шаг `0.183594` является дискретизацией 25 кадров гладкого
`smoothstep`; прямой ряд весов монотонен от `0` до `1`, обратный совпадает,
caster-role не меняется.

Отклонённый перенос в `level_e` теперь доступен только диагностическому
validator и маршрутному runner:

```text
Godot --headless --path . \
  --script res://tools/validate_level_e_light_zones.gd
Godot --path . \
  --script res://tools/run_level_e_light_zone_boundaries.gd
```

Bot/debug snapshot обязан сохранять
название режима, число зон/порталов, размер постоянного caster-набора,
active-shadow count и source-energy signature. Внутри одной зоны при
неизменной topology допустимо `0` смен caster-signature; на portal crossing
энергия должна меняться монотонно и одинаково в прямом/обратном направлении.
Маршрутный Forward+ stress включает все проходимые межгрупповые стыки;
лимит — 11 теней, device loss запрещён.

Первый level_e checkpoint `2026-07-28 22-44-40`: data/runtime validators
прошли `28` anchor-групп без forward/reverse mismatch, хаб держит `11`
активных теней. Forward+ переход через `infinite_e` завершился штатно,
`passed=true`, max main commit `0.642 мс`, renderer/device loss отсутствует.
Визуальный допуск пока не выдан.

### Маршрутный A/B по границам областей level_e

Статический validator планов не является визуальным допуском `ZONE-11`.
Отдельный runner обязан пройти в обоих направлениях каждый проходимый стык
между разными `area_group` реального `level_e`, удерживая камеру вдоль
маршрута. Для одной и той же последовательности мировых координат отдельно
снимаются `LF3-11F` и `ZONE-11`.

Runner сохраняет для каждого кадра текущую группу, energy/caster-signature,
число теней, среднюю яркость кадра и максимальный межкадровый скачок. Для
наихудших стыков сохраняются контрольные кадры до границы, на границе и после
неё. Обязательные критерии:

- соседняя освещённая область не может оставаться полностью подавленной до
  пересечения геометрической границы;
- прямой и обратный проход в одинаковой координате дают одинаковое состояние;
- профиль не превышает 11 теней;
- `ZONE-11` не увеличивает occupancy leak-risk относительно `LF3-11F`;
- смена anchor-плана не создаёт отдельный скачок энергии или яркости.

Запуск:

```text
Godot --path . --script res://tools/run_level_e_light_zone_boundaries.gd
```

До прохождения этого runner и ручного просмотра кандидат не может заменить
`LF3-11F`.

Baseline `2026-07-28 23-26-32` прошёл `35` реальных стыков в обоих
направлениях и воспроизвёл ручное замечание. `ZONE-11` сохранил лимит 11 и
пространственную симметрию, но при смене anchor-плана дал скачок суммы
energy-weight `85.0`; max leak-risk вырос с `2.087817` у `LF3-11F` до
`2.988241`. Вариант с caster-focus по центру anchor-группы отклонён.
Следующий кандидат обязан использовать исходный portal-aware ranking.

Portal-only промежуточный вариант не дошёл до маршрутного прогона: базовый
validator обнаружил `0` полезных активных теней в центре хаба. Следующий
runner проверяет смешанный ranking с одной общей квотой 11: большая половина
для anchor-интерьера, остаток для порталов.

Повтор смешанного ranking `2026-07-28 23-35-21` дал те же провальные значения:
`energy-step=85.0`, `max leak-risk=2.988241`. Кандидат для общего `level_e`
окончательно отклонён; ручной `V` и CLI-старт удалены. Runner сохраняется как
регрессия, доказывающая границу применимости лабораторного алгоритма.

Отдельный прогон не сравнивает общую похожесть изображения, а изолирует
источник света на комнатной стороне сплошной части входной перегородки:

```text
Godot --path . infinite_corridor_e.tscn -- --lf3-leak-capture
```

Бот использует фиксированный вид `УГОЛ / ПЕРЕГОРОДКА` и последовательно
снимает:

1. полностью тёмный контроль;
2. ambient без ламп и эмиссивной панели;
3. только эмиссивную панель сюжетной лампы;
4. только сюжетную лампу при уровне мерцания `0.08`;
5. только коридорные лампы с действующим shadow-пулом;
6. действующий shadow-пул, но с полной непрозрачностью уже выбранных теней;
7. все способные дотянуться коридорные лампы с прежней мягкой
   непрозрачностью `0.74`;
8. те же коридорные лампы с полной непрозрачностью теней у всех источников;
9. тот же контроль с уменьшенными bias только как диагностику отделения тени
   от края — эта настройка не является кандидатом на перенос.
10. полный наблюдаемый кадр `DIM 0.08` для связи изолированных вкладов с
   исходным визуальным дефектом.

Для каждого кадра отчёт сохраняет среднюю яркость контрольной области в
верхней части комнатной стороны перегородки. Коридорный вклад считается
пробоем, если после вычитания `ambient` он остаётся визуально различимым за
сплошным участком стены. Совпадение `LEGACY/LF3` не является допуском: этот
тест должен отдельно доказать блокировку стены. Выходной каталог:

```text
.lf3_leak/<timestamp>/
```

Прогон диагностический: он обязан восстановить все параметры света, тени,
панель, ambient, камеру и режим после завершения.

Вариант `lf3_occlusion_suppress` сохраняет штатные Omni и бюджет LF3, но
измеряет результат после подавления только незащищённых occupancy-risk
источников. Для него обязательны sealed, open-control и двунаправленный
motion-прогон: светлый маршрут не должен менять фотометрию, а поворот камеры —
состав активных источников.

### Зубчатая толстая стена AreaSpec

Для `perimeter_teeth_hall` используется отдельный Forward+ прогон:

```text
Godot --path . --script res://tools/run_perimeter_teeth_light_ab.gd
```

Он снимает одинаковые ракурсы из центра основного зала и вблизи северных,
западных, восточных и южных зубьев. На каждом ракурсе последовательно
изолируются `ambient`, прямоугольная панель, bounce без теней, bounce со
штатным `LF3-11F`, варианты LF3/all-shadow с низким bias, контроль с высотой
источника из `infinite_e` и полный штатный свет. Кроме специального
height-контроля геометрия, камера, энергия и позиции источников между
вариантами не меняются. Отчёт сохраняет общую, центральную и верхнюю
ROI-яркость, число активных bounce-теней и contact sheet.

Артефакты записываются в `.perimeter_teeth_light_ab/<timestamp>/`.

Предварительный прогон `2026-07-26T20-11-47` подтвердил источник засвета,
но не является допуском: он ошибочно использовал базовый distance-only пул
вместо override `LF3-11F` из `level_e`.
В нём
`panels_only` пиксельно совпадает с `ambient`, значит прямоугольная панель с
default `area_range=0.05` не участвует. Свет даёт только bounce-Omni. В
тупиковом северном канале ROI-вклад bounce относительно ambient уменьшается
штатным пулом примерно на `21%`, а принудительными тенями всех достижимых
источников — на `36%`; около `64%` вклада остаётся. Из центра зала
принудительные тени уменьшают вклад менее чем на `1%`. Следовательно, это не
ошибка выбора shadow-slot. Итоговый вывод допускается только после повторного
прогона с `LF3-11F`, low-bias и height-контролями.

Повторные прогоны `2026-07-26T20-23-30/20-25-51/20-28-25` выполнены с
настоящим override `LF3-11F` из `lighting_module.gd`. Непрерывный потолок над
wall-клетками устраняет архитектурное расхождение с `level_e`, но на данном
ракурсе меняет ROI менее чем на `0.2%`: верхней геометрической щели здесь не
было. Low-bias уменьшает верхний вклад примерно на `16–22%`, но не убирает
его; высота источника из `infinite_e` вклад увеличивает.

Изоляция регионов показала, что свет пристыкованного северного зала составляет
около `6%` наблюдаемого вклада внутри тупика и почти полностью подавляется
LF3. Доминируют две лампы ближнего ряда самого основного зала. Дальний ряд
пиксельно совпадает с ambient внутри тупика. При неизменной раскладке
`bounce_range=6 м` также даёт точное совпадение с ambient, тогда как штатные
`8 м` освещают тупик напрямую через открытый рот. Это отличает текущий случай
от прежнего пробоя сквозь закрытую перегородку: защитный LF3 работает, но не
может затенить геометрически открытую линию источник→receiver. Следующий
кандидат — отнести ближний ряд глубже в зал либо локально ограничить bounce
для светильников у зубчатого периметра; глобальный канон света не менять без
отдельного допуска.

Проверка размещения `2026-07-26T20-33-02`: сдвиг четырёх ламп основного зала
на одну клетку к центру при неизменных `level_e`-параметрах уменьшает
bounce-вклад внутри тупика примерно на `87%`. Локальный `range=6 м` убирает
его до точного ambient, но меняет профиль источника. Поэтому предпочтительный
первый кандидат для игрового прототипа — центральная раскладка, а не локальная
перенастройка света.

## A/B профиля теней в level_e

Отдельный runner проверяет перенос LF3-8O в реальный occupancy-уровень:

```text
Godot --path . level_e.tscn -- --lf3-level-e-capture
```

Runner фиксирует `maze_seed`, переносит игрока и активный световой пул в четыре
безопасных центра ячеек `MAZE_AFTER_PIT_CELL`/`MAZE_AFTER_PIT_TAIL_CELL`,
достраивает стриминговое окно и для
каждого вида снимает `REFERENCE | LF3-8O`. Геометрия, материалы, ambient,
энергия и range между сторонами не меняются. Выход:

```text
.lf3_level_e/<timestamp>/
```

В `report.json` сохраняются время установившегося кадра, RGB MAE, отношение
яркости и состояние shadow-пула. После статических пар runner выполняет 900
Vulkan-кадров непрерывного движения, фиксируя пик активных теней и mean/max
frame time; пик выше 10 немедленно останавливает тест. Поле переходных кадров
сохраняется для истории вариантов fade. При первом частичном пространственном
переходе runner останавливает игрока на 30 кадров и требует нулевого изменения
progress. После прогона runner обязан вернуть
`REFERENCE`, игрока, игровую камеру, HUD, миникарту и размер окна.

Ручное сравнение в обычном `level_e`: клавиша `8`, статус виден в HUD.

### Коробки у стены: направленный тест границы теней

Для воспроизводимого отлова включения/выключения теней у трёх коробок под
стрелкой в хабе используется отдельный runner:

```text
Godot --path . level_e.tscn -- --lf3-level-e-box-shadow-capture
```

Камера движется вместе с игроком и постоянно смотрит на коробки, проходя 31
одинаковую точку от центра зала к стене и затем те же точки обратно. Полный
маршрут отдельно повторяется в `REFERENCE` и LF3 без переключения режима
внутри направления. Для измерения гистерезиса бот попарно сравнивает подход и
отход в одной и той же мировой координате (зеркальные индексы пути): камера и
перспектива совпадают, меняется только история направления shadow-пула.

Runner сохраняет четыре последовательных contact sheet (`REFERENCE/LF3 ×
approach/retreat`), пары кадров вокруг максимального скачка и `report.json` с
расстоянием до коробок, ROI-luma, межкадровым RGB MAE, направленным MAE в
одинаковых координатах и состоянием shadow-пула.
Выходной каталог:

```text
.lf3_level_e_boxes/<timestamp>/
```

Тест диагностический: он не меняет параметры света и восстанавливает
`REFERENCE`, игрока, камеру, HUD, миникарту и размер окна.

Контроль stateless-профиля `2026-07-18 01-46-04`: data-validator дал `0`
расхождений на 21 зеркальной позиции и пик `10` теней; направленный визуальный
пик коробочного runner — `RGB MAE=0.000682` против `0.00213` у отклонённого
stateful-варианта `3 м`.

### Коробки у стены: A/B плавности без движения камеры

Для сравнения именно проявления/затухания теней, без примеси перспективы,
используется отдельный runner:

```text
Godot --path . level_e.tscn -- --lf3-level-e-box-shadow-smoothness-capture
```

Камера фиксирована в близком ракурсе на коробки. Только позиция игрока проходит
один и тот же прямой маршрут с равным пространственным шагом сначала к стене,
затем обратно; маршрут повторяется для `REFERENCE` и LF3. В отчёт входят
межкадровый RGB MAE, ROI-luma, shadow-состояние, пик/медиана/p95 изменения и
сравнение зеркальных координат. Такой тест измеряет изменение освещения и теней,
а не перемещение объектов в кадре. Он диагностический и не меняет профиль света.

Прогон `2026-07-18 22-22-11` с шагом `0.190625 м` подтвердил визуальный pop
LF3. Пиковый межкадровый RGB MAE равен `0.00037957` в обоих направлениях,
тогда как у `REFERENCE` — `0.00001243` (примерно в 30.5 раза меньше).
В точке пика две лампы обмениваются полным и граничным слотами: их opacity за
один шаг меняются примерно `0.72 → 0.02` и `0.01 → 0.74`. Направленный MAE в
одинаковых координатах остаётся низким (`0.00001259`), поэтому это не
гистерезис, а симметричный скачок идентичности shadow-caster.

После визуальных последовательностей runner дополнительно выполняет 900 кадров
движения по тому же маршруту без захвата изображений и записывает пик активных
теней, число кадров с 11 тенями и mean/max frame time. Это Vulkan-стресс именно
переходной зоны, а не общего маршрута maze, где одиннадцатый слот может ни разу
не понадобиться.

Вариант с временной 11-й тенью проверен `2026-07-18 22-35-41`: пик плавности
LF3 снизился до `0.00021379` против `0.00037957` у лимита 10; подход и отход
совпали. Vulkan-стресс: 900 кадров с 11 тенями, `mean=16.414 ms`,
`max=23.692 ms`, сбоя устройства нет.

После добавления ручного переключателя `0` fixed-camera runner сравнивает три
профиля в одной сессии: `REFERENCE`, `LF3-10J`, `LF3-11P`. Для каждого он
снимает подход/отход и выполняет отдельный 900-кадровый FPS-прогон.
Дополнительно записывается occupancy leak-risk: сумма энергии активных ламп,
чьи сегменты к receiver-пробам пересекают стену/перегородку, но сами лампы не
имеют shadow-map. Это машинный относительный индикатор между профилями, а не
абсолютное доказательство отсутствия видимого засвета.

Основной `--lf3-level-e-capture` также снимает три профиля на каждом из четырёх
maze-ракурсов и сохраняет отдельные `REFERENCE↔10J` и `REFERENCE↔11P` пары.
Для каждого статического кадра отчёт содержит тот же occupancy leak-risk, чтобы
дальняя maze-проверка не подменялась маршрутом у коробок.

Единый тройной прогон `2026-07-19 01-38-10`: fixed-camera peak
`REFERENCE/10J/11P = 0.00001243/0.00037957/0.00026176`; `11P` уменьшает
главный pop `10J` на 31%, направления у всех профилей совпадают. Средний кадр
на 900 одинаковых шагах `14.904/14.826/14.811 ms` — различие среднего в
пределах 0.7%, но worst frame у `11P` выше: `26.97 ms` против `19.60/18.93`.
Средний occupancy leak-risk `0.28324/0.02468/0.00635`: оба LF3 ниже
REFERENCE, `11P` ниже `10J`, но не равен нулю.

Статический maze-прогон `2026-07-19 01-41-36` закрыл все 5–8 локальных
receiver-проб у обоих LF3, хотя ручной дальний засвет остаётся. Следовательно,
локальный веер вокруг игрока недостаточен для дальних видимых поверхностей;
нулевой probe-score нельзя считать отсутствием засвета.

После появления `LF3-11F` тройной runner использует
`REFERENCE/LF3-10J/LF3-11F`. `LF3-11P` не перезаписывается: его последний
checkpoint `2026-07-19 01-38-10/01-41-36` остаётся базой сравнения. Для `11F`
обязательно записывать отдельно число локальных и дальних frustum-receiver.

Первый полный результат `11F`: fixed-camera/stress
`2026-07-19 01-57-59`, static maze `2026-07-19 01-59-56`. В сравнении с
`10J` профиль уменьшил peak проявления тени на 41% и средний far leak-risk на
37.8%, но увеличил среднее время stress-кадра с `13.80` до `14.90 ms`.
Статический отчёт обязан считаться по каждому виду, а не только по среднему:
остаток unshadowed caster для `10J→11F` равен `1→0/0→0/1→1/2→1`.
Следовательно, итог бота — «направление подтверждено, частичный остаток есть»;
это не автоматический финальный допуск.

Vulkan/FPS серия лучшего визуального кандидата `11F` (`2026-07-19`):

- 5 последовательных stress-запусков, по 900 кадров на профиль;
- у `11F` 4500/4500 кадров с 11 активными тенями;
- 3 дополнительных maze/motion запуска со сменой ракурсов;
- штатное завершение `8/8`, `VK_ERROR_DEVICE_LOST = 0`;
- среднее время stress-кадра по пяти сессиям:
  `REFERENCE/10J/11F = 14.292/14.196/14.236 ms`;
- worst frame всей серии: `21.86/33.37/31.35 ms`.

Один наблюдавшийся пользователем device loss сохраняется как отдельный факт.
При `0/8` автоматических воспроизведений нельзя ни привязать его к `11F`, ни
объявить случайным. Следующий Vulkan-инцидент должен фиксироваться вместе с
профилем, числом активных теней, позицией/направлением камеры и фактом
стриминга в последние кадры; иначе зависимость останется неразличимой.

## Порядок допуска изменения света

1. Сначала data-only проверки occupancy/solver.
2. Затем статический A/B и просмотр всех контактных листов.
3. Затем динамический прогон с indirect.
4. При лаге — контроль `--no-indirect`, чтобы отделить solver/renderer от
   чанков, direct и shadow.
5. После машинного допуска — ручной проход в `LEGACY/LF3` клавишей `8`.
6. Для изменений видимости direct обязательна отдельная диагностика пробоя;
   общий RGB-паритет с текущим `LEGACY` её не заменяет.
7. Результат и timestamp фиксируются в `docs/lighting_field.md` и кратко в
   `docs/plan_areas_v2.md`.

Изменение отклоняется и полностью снимается, если появляется хотя бы одно:

- чёрная/плоская грань, новый шов, исчезновение плинтуса или проёма;
- свет за закрытой occupancy-границей;
- таймерное изменение света при неподвижной камере;
- видимый скачок direct/shadow или света при рециклинге;
- отдельный кадровый пик либо заметная регрессия установившегося кадра;
- необходимость локальной настройки конкретной комнаты вместо общего правила.

Для сюжетного рециклинга `infinite_e` используется отдельный профиль:

```text
Godot --path . --script res://tools/run_infinite_story_recycle_profile.gd
```

Он выполняет `story swap`, проходит 120 м и отдельно помечает кадр удаления
постановочной комнаты. Отчёт сохраняет main-thread commit и worker-профиль
LF3. Допуск: на кадре удаления нет полного solver-времени и отдельного пика
относительно соседних кадров; затем обычный motion/recycle тест подтверждает
неизменность периодического коридора.

Принятый Forward+ результат `2026-07-27T19-53-29`: кадр удаления
`16.24 мс` при медиане `16.67 мс`; прежний пик полного solver был около
`172 мс`. Renderer обновляет texture/mapping уже привязанного материала и
добавляет только новые floor-bindings, поэтому тот же commit применим к
расширению топологии областью-шлюзом.

После добавления distance blend Forward+ прогон `2026-07-27T20-19-46`
подтвердил монотонный вес `0→1` на `50…65 м`; кадр удаления на `67 м`
`16.69 мс` при медиане `16.61 мс`. Пик blend-диапазона `18.01 мс`, отдельного
скачка перед удалением или на нём нет.

Числа сравниваются прежде всего с соседним LEGACY-прогоном на той же машине:
нагрев MacBook влияет на абсолютное время. Низкий RGB MAE не оправдывает
performance-регрессию, а хороший frame time не оправдывает плохую картинку.

## Текущая принятая точка

Последняя принятая rendered-конфигурация — LF3-8:

- неподвижное изменение энергии: `0`;
- 100 м движения и 10 рециклингов без скачка энергии;
- direct определяется расстоянием, не таймером;
- статический LEGACY/LF3 визуально совпадает;
- после отката LF3-9 контроль `2026-07-17T00-51-11` дал медианы
  `13.57/14.09 мс`, максимальный RGB MAE `0.00392`.

Поверх LF3-8 в `infinite_e` зафиксирована лабораторная окклюзия LF3-8O:
10 стабильных теней с opacity `1.0`, blur `2.75` и исходными bias. Leak-контроль
убирает коридорный вклад на сплошной перегородке `0.07655 → 0.0`; blur `2.75`
выбран после матрицы `2.75/3.0/3.5/4.0/6.0`, чтобы не скруглять плинтус и не
сливать тень стула. Финальные контроли: leak `2026-07-17T21-35-20`, motion
`2026-07-17T21-36-14` (`16.51/17.41/21.69 мс`, 20 смен shadow-пула,
10 рециклингов). Это ещё не общий допуск: maze и `level_e` не проверены.

Два LF3-9 стеновых renderer отклонены ботом и удалены. Следующая попытка стен
разрешена только внутри общего occupancy-builder, без индивидуальных
материалов или mesh на стену.
